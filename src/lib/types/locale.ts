export type Dictionary = {
    companyName: string
    components: {
        header: {
            title: string
            github: string
            discord: String
        }
        footer: {
            Icon: string
            LibreTranslate: string
        }
    }
}
