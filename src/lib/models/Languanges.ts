export type Language = {
    code: LanguageCode
    name: string
}

export enum LanguageCode {
    Auto = 'auto',
    English = 'en',
    Chinese = 'zh',
    Germany = 'de',
    Polish = 'pl',
    Spanish = 'es',
}
